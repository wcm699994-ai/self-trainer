# SelfTrainer 使用说明

## 1. 开始使用

打开应用后，建议先在「配置」页导入一套模板：

1. 点击「配置」
2. 在内置模板中选择一个：作息校准版、学生备考版、职场效率版
3. 点击「预览」查看指标说明
4. 点击「导入」即可生效

模板中的指标分为两类：

- **损失指标（越小越好）**：如屏幕使用时长、未完成任务数
- **增益指标（越大越好）**：如运动时长、专注时长

## 2. 每日录入

进入「今日训练」页：

1. 根据指标名称，在数字输入框中填写当日实际数值
2. 可使用 +5 / +10 / -5 / -10 快捷按钮减少输入成本
3. 如果今天与昨天情况相同，点击「复制昨天」一键填充数值（标签不会复制）
4. 可选添加标签：正常、生病、突发事件
5. 点击「提交」

提交后，系统会立即生成一条今日梯度建议。

## 3. 补录数据

在首页顶部可以切换日期：

- 点击「昨天」快速补录
- 也可以选择近 7 天内任意日期

补录数据会被标记为补录，周复盘时会提示样本可信度。

## 4. 查看复盘

进入「复盘」页：

- 查看近 7 天或近 30 天损失/增益趋势曲线
- 阅读基础复盘结论
- 如已配置 API Key，可手动展开「AI 深度复盘」
- 若 AI 失败，可点击「重试」按钮

## 5. 数据备份

在「配置」页底部：

- 导出 JSON 备份：完整保存指标配置和全部记录
- 导入 JSON 备份：恢复数据（原子替换）
- 导出 CSV：用于外部查看分析
- 一键重置：清空所有数据

## 6. AI 配置（可选）

SelfTrainer 已固定使用 DeepSeek 官方服务，无需填写 API 地址和模型名称。

只需在「配置」页填写 DeepSeek API Key，点击「保存」，再点击「测试连接」确认可用。

仅在两处可手动触发 AI：

- 配置页：AI 帮我生成指标
- 复盘页：AI 深度复盘

## 7. 指标修改注意事项

- 编辑指标名称时，系统会自动迁移历史数据中的对应数值
- 删除指标后，历史数据中该指标的值将不再显示
- 目标值设为 0 时仅作记录，建议设置合理基准值以获得有效调参建议

## 核心理念

SelfTrainer 将行为优化视为模型训练过程：

- 每日录入 = 产生训练样本
- 梯度建议 = 小步调整，防止震荡
- 周复盘 = 验证集评估

用客观数据替代主观感受，用工程化方法降低内耗。
